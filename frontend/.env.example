# Frontend environment template. Copy to .env (gitignored) to override.
# Only VITE_-prefixed vars are exposed to the client by Vite.

# Base URL of the backend API.
VITE_API_URL=http://localhost:3000/api/v1
